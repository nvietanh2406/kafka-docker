{
    "name": "postgres-customers-sink",
    "config": {
        "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
        "tasks.max": "1",
        "topics": "mysql-source.sourcemysqldb.customers",
        "connection.url": "jdbc:postgresql://postgres:5432/targetdb",
        "connection.user": "debezium",
        "connection.password": "dbz123",
        "auto.create": "true",
        "auto.evolve": "true",
        "insert.mode": "upsert",
        "delete.enabled": "true",
        "pk.mode": "record_key",
        "pk.fields": "id",
        "table.name.format": "customers"
    }
}
